extend layout

block content
	div(class="container m-2")
		div(class="list-group")
		    if posts
		        each post, i in posts
		            .post  
		                a(class="list-group-item list-group-item-action" href='/'+post.blog_id)
		                    =post.title